'use client'

const TasksPage = ({ params: { lng } }: { params: { lng: string } }) => {

    return (
        <>
            <section>
            </section>
        </>
    )
}

export default TasksPage